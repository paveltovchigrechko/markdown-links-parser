---
title: Optimum
sidebar_position: 20
description: Optimum is default Unistore theme for your store.
keywords:
    - Unistore theme
    - optimum
    - header
    - logo
    - slideshow
    - slide
    - grid products
    - grid collection
    - display collection
    - list of collections
    - collection section
    - product section
    - footer
---

import Image from "@site/src/components/Image";

This article describes the sections of Optimum Unistore theme:
* [Header](#header)
* [Slideshow](#slideshow)
* [Grid products](#grid-products)
* [Grid collection](#grid-collection)
* [Display collection](#display-collection)
* [List of collections](#list-of-collections)
* [Product](#product-section)
* [Collection](#collection-section)
* [Footer](#footer)

By default, Optimum is set as active for all created stores.

## Header {#header}

:::info

In Optimum, the header section settings apply to all pages. Therefore, once you set up the header for one page (for example, for the home page), other pages (product and collection pages) will render the header with the same settings.

:::

The **Header** is the topmost section on the page.

The **Header** section contains [**Logo**](#logo), [**Links**](#links), and [**Announcement bar**](#announcement-bar) settings.

### Logo {#logo}

The **Logo** settings define the image that is rendered on the left side of the header.

**Image**. You can add the logo image in three ways:
* Select the frame and choose the file in the file explorer
* Drag and drop the image file on the frame
* Select **Image gallery**, select the image, and choose **Select**
    
**Logo name**. This text appears if the logo image cannot be rendered for some reason.

### Links {#links}

The **Links** settings allow you to change the [navigation menu](../navigation.mdx) used in the section. By default, the **Links** settings contain the **Header** navigation menu. 

To edit the navigation menu, select <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} /> in **Navigation menu**.

To change the navigation menu:
1. In **Navigation menu**, delete the name of the current navigation menu
2. In **Navigation menu** drop-down list, select the new navigation menu

### Announcement bar {#announcement-bar}

The **Announcement bar** settings define the topmost bar that appears above the header. You can use this bar to bring the attention of your customers to an announcement. The announcement bar works as a link that redirects customers to a specified URL. For example, you can redirect customers to the page of a collection on sale.

**Show announcement**. Mark the checkbox to enable the announcement bar.

**Home page only**. Mark the checkbox to restrict the announcement to the home page only. If not marked, the announcement appears on all website pages.

**Text**. Fill in the text that will appear in the announcement bar. The text will be a centered link.

:::tip

You can add emojis in the announcement bar text.

:::

**Link**. Fill in the URL of the announcement text. Upon selecting the announcement text the customer will be redirected to this URL.

**Background**. Select the announcement background color in the color picker.

**Text**. Select the announcement text color in the color picker.

## Slideshow {#slideshow}

**Slideshow** is a section that contains up to 16 slides with images. You can set up images, text, and interactive buttons for the slides so your customers can get additional information about your products. You can set auto-rotation for slides or let your customers rotate them manually.

The **Slideshow** section contains the slides list (learn [how to manage slides](#manage-slides)) and general settings:
* **Slide height**. Select the height of the slides in the drop-down menu
* **Text size**. Set the size of the text that you add to the slides in the drop-down menu 
* **Text alignment**. Set the alignment of the text that you add to the slides in the drop-down menu
* **Blur image**. Mark the checkbox to enable the blurring of the slide image
* **Auto-rotate slides**. Mark the checkbox to enable the slides' auto-rotation on the website. If the checkbox is not marked, your customers have to rotate the slides manually
* **Rotation time, sec**. Move the slider to set up the time of the slides' auto-rotation

### Manage slides {#manage-slides}

The slides in the **Slideshow** section are displayed in the sidebar. The slides appear in the **Slideshow** section of the website as they appear in the sidebar: the topmost slide in the sidebar appears as the first one on the website, and so on.

To add a new slide:
1. Select **Add slide**. The settings of the new slide appear in the sidebar
2. Set settings for the new slide (learn more in [**Slide settings**](#slide-settings))

To edit slide settings:
1. Select the slide panel. The settings of the slide appear in the sidebar
2. Change settings for the slide (learn more in [**Slide settings**](#slide-settings))

To change a slide position in the slideshow:
1. In the slide panel, select and hold <Image src="/img/drag-button.png" alt="Drag button" width={20} height={20} />
2. Move the slide in the list and release it

To delete a slide:
1. In the slide panel, select <Image src="/img/delete-icon.png" alt="Delete button" width={20} height={20} />
2. In the pop-up window, select **Delete**

Alternatively, to delete a slide:
1. Select a slide
2. In the upper panel, select <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} />
3. Select **Delete**
4. In the pop-up window, select **Delete**

To return to the **Slideshow** section:
* for desktop mode use <Image src="/img/website-editor-return-button.png" alt="Return button" width={20} height={20} /> in the sidebar upper panel
* for tablet and mobile modes use <Image src="/img/website-editor-return-button.png" alt="Return button" width={20} height={20} /> in the upper panel

### Slide settings {#slide-settings}

The slide settings are organized as groups.

**Image** settings. You can add the slide image in different ways:
* Select the frame and choose the image file in the file explorer
* Drag and drop the image file on the frame (for the desktop version)
* Select **Image library**, select the image, and choose **Select**

**Text** settings contain all text related parameters:
* **Heading**. Fill in the text heading
* **Subheading**. Fill in the text that appears on the slide
* **Text**. Select the text color in the color picker

:::note

The text position on the slide is defined in the **Slideshow** section settings.

:::

**Button** settings contain button-related parameters:
* **Button text**. Fill in the button text
* **Button link**. Fill in the button link. After your customers select the button, they will be redirected to this link. Format: `#product-page`
* **Text**. Select the color of the button text in the color picker
* **Background**. Select the button color in the color picker

## Grid products {#grid-products}

**Grid products** is a section that displays up to 16 products as a tile grid. You can select the products to display and customize the grid. Each title contains the product image and parameters and serves as a link to the product page.

The **Grid products** contains the products list (learn [how to manage products](#manage-products)) and general settings.

**Settings** contain the section-related parameters:
* **Headline**. Fill in the section headline. The headline appears above the grid
* **Number of products in a row**. Move the slider to set the number of products in a row

**Styles** settings contain the parameters related to the product tile in the grid:
* **Heading**. Select the color of the product title in the color picker
* **Subheading**. Select the color of the product description in the color picker
* **Price**. Select the color of the product actual price in the color picker
* **Hover**. Select the color of the frame of the product tile in the color picker. The frame appears upon hovering over the tile

### Manage products {#manage-products}

The products in **Grid products** section are displayed in the sidebar. The products appear in the **Grid products** section of the website as they appear in the sidebar: the topmost product in the sidebar appears as the first to the left one on the website, and so on.

To add a new product:
1. In the sidebar, select **Add product**. The field for the product will appear in the sidebar
2. Select the field and choose the existing product to display in the section

Alternatively, you can [create a new product](../../products/products.mdx#create-product) by selecting the **Create product** line in the drop-down menu. Also, you can edit a selected product by selecting <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} />. 

To change a product tile position in the grid:
1. In the product panel, select and hold <Image src="/img/drag-button.png" alt="Drag button" width={20} height={20} />
2. Move the panel in the list and release <Image src="/img/drag-button.png" alt="Drag button" width={20} height={20} />

To delete a product tile:
1. In the product panel, select <Image src="/img/delete-icon.png" alt="Delete button" width={20} height={20} />
2. In the pop-up window, select **Delete**

Alternatively, to delete a product tile:
1. Select a product tile
2. In the upper panel, select <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} />
3. Select **Delete**
4. In the pop-up window, select **Delete**

To return to the **Grid products** section:
* for desktop mode use <Image src="/img/website-editor-return-button.png" alt="Return button" width={20} height={20} /> in the sidebar upper panel
* for tablet and mobile modes use <Image src="/img/website-editor-return-button.png" alt="Return button" width={20} height={20} /> in the upper panel

## Grid collection {#grid-collection}

The **Grid collection** section displays the products of a collection as a grid. The collection products appear in alphabetical order.

The **Grid collections** also contains **Browse all products** button below the grid. The button redirects the customer to the collection page.

The **Grid collection** settings:
* **Headline**. Fill in the section headline. The headline appears above the grid
* **Collection**. Select the collection whose products will appear in the section in the drop-down menu. You can also:
    * Create a new collection. Select **Create item** 
    * Edit the chosen collection. Select <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} />

:::tip

Check out [how to create](../../products/collections/collections.mdx#create-collection) and [to edit a collection](../../products/collections/collections.mdx#edit-collection).

:::

* **Number of products in a row**. Move the slider to set the row width. A row can contain 2-5 products
* **Number of rows**. Move slider to set the number of grid rows. Grid collection can have up to 5 rows

:::caution

Please, be aware that the **Grid collection** shows only the number of products that equals the multiplication of **Number of products in a row** and **Number of rows**. If the collection has more products than given number, the section does not show the rest of the products!

:::

## Display collection {#display-collection}

The **Display collection** section displays the first five products of a collection as a grid. The section displays the products in alphabetical order  

If the collection has more than five products, the **Display collection** also contains **Browse all products** button below the grid. The button redirects to the collection page.

The **Display collection** settings:
* **Headline**. Fill in the section headline. The headline appears above the grid
* **Collection**. Select the collection whose products will appear in the section in the drop-down menu. You can also:
    * Create a new collection. Select **Create item**
    * Edit the chosen collection. Select <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} />

:::tip

Check out [how to create](../../products/collections/collections.mdx#create-collection) and [to edit a collection](../../products/collections/collections.mdx#edit-collection).

:::

## List of collections {#list-of-collections}

The **List of collections** section displays up to 12 collections as a tile grid. You can select the collections to display and customize the grid. Each tile contains the collection image and title and serves as a link to the collection page.

The **List of collections** section contains the collections list (learn [how to manage collections](#manage-collections)) and settings:
* **Headline**. Fill in the section headline. The headline appears above the grid
* **Collections in row**. Move the slider to set the number of collections for a grid row

### Manage collections {#manage-collections}

The collections in **List of collections** section are displayed in the sidebar. The collections appear in the **List of collections** section of the website as they appear in the sidebar: the topmost collection in the sidebar appears as the topmost one on the website, and so on.

To add a new collection:
1. In the sidebar, select **Add collection**. The field for the new collection will appear in the sidebar
2. Select the field and choose the existing collection to display in the section

Alternatively, you can edit a collection by selecting <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} />. You can also [create a new collection](../../products/collections/collections.mdx#create-collection) by selecting the **Create item** line in the drop-down menu.

To change a collection in a grid tile:
1. Select the collection panel in the sidebar. The drop-down menu for the collection appears in the sidebar
2. Select a new collection for the tile

To change a collection tile position in the grid:
1. In the collection panel, select and hold <Image src="/img/drag-button.png" alt="Drag button" width={20} height={20} />
2. Move the panel in the list and release <Image src="/img/drag-button.png" alt="Drag button" width={20} height={20} />

To delete a collection tile:
1. In the collection panel, select <Image src="/img/delete-icon.png" alt="Delete button" width={20} height={20} />
2. In the pop-up window, select **Delete**

Alternatively, to delete a collection tile:
1. Select a collection tile
2. In the upper panel, select <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} />
3. Select **Delete**
4. In the pop-up window, select **Delete**

To return to the **List of collections** section:
* for desktop mode use <Image src="/img/website-editor-return-button.png" alt="Return button" width={20} height={20} /> in the sidebar upper panel
* for tablet and mobile modes use <Image src="/img/website-editor-return-button.png" alt="Return button" width={20} height={20} /> in the upper panel

## Product {#product-section}

The **Product** section is a part of the product page. It displays the product information and the button to add the product to the cart.

The **Product** section settings:
* **Allow image zooming**. Mark the checkbox if you want your customers to be able to zoom the product images
* **Show comparative price**. Mark the checkbox to display the product comparative price
* **Show whole description**. Mark the checkbox to display the full product description
* **Show vendor name**. Mark the checkbox to display the product [vendor](../../products/vendors/vendors.mdx) name
* **Show quantity selector**. Mark the checkbox to display the quantity selector for the product items. Your customers will be able to add multiple product items to the cart
<!-- * **Show sharing buttons**. Find more in [Social sharing buttons](../../preferences/SEO/on-page-seo.mdx#social-sharing-buttons) -->

## Collection {#collection-section}

The **Collection** section is a part of the default collection page template. It displays the collection products and optionally its image and description.

The **Collection** section settings:
* **Show collection banner**. Mark the checkbox to display the collection image at the top of the section
* **Show collection description**. Mark the checkbox to display the collection description. It appears at the top of the section. If you mark **Show collection banner** checkbox, the description appears on the collection image
<!-- Recommendations for the collection image since the description color cannot be changed -->
* **Number of products in a row**. Move the slider to set the collection product number for the grid row

## Footer {#footer}

:::info

In Optimum, the footer section settings apply to all pages. Therefore, once you set up the footer for one page (for example, for the home page), other pages (product and collection pages) will render the footer with the same settings.

:::

The footer is the lowermost section on the page.

The **Footer** section in the sidebar contains **Links** settings and **Show payment icons** checkbox. 

The **Links** settings allow you to change the [navigation menu](../navigation.mdx) used in the section. By default, the **Links** settings contain the **Footer** navigation menu. 

To edit the navigation menu, select <Image src="/img/settings-button-2.png" alt="Settings button" width={20} height={20} /> in **Navigation menu**.

To change the navigation menu:
1. In **Navigation menu**, delete the name of the current navigation menu
2. In **Navigation menu** drop-down list, select the new navigation menu

Mark the checkbox to display the icons of the payment methods available on your website.

:::note

You can set the payment methods in [**Payments**](../../store-settings/payments/payments.mdx).

:::